apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Govna.fullname" . }}-test-connection"
  labels:
    {{- include "Govna.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Govna.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
